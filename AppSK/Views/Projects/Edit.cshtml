@using AppSK.Models.Projects
@model ProjectModel
@{
    ViewBag.Title = "Редактирование проекта";
}

<h2>@ViewBag.Title</h2>

<div class="row">
    <div class="ms ms-form-area col-md-12">
        @using (Html.BeginForm("Update", "Projects", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.Partial("_Form", Model)
        }
    </div>
</div>
